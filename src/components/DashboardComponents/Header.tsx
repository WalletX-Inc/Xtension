import React from "react";

const Header = () => {
  return (
    <>
      This is for quick settings and switching accounts A modal should be there
      for the profile icons including 3 buttons 1. edit profile 3. Lock 
      
      There should be a circular tab for selecting the accout type for transactions

      Add a notification page at the end
    </>
  );
};

export default Header;
